version: 2
# Version updates disabled: Renovate (org-level config in .github/renovate.json)
# handles all dependency and GitHub Actions updates, including SHA pinning and
# auto-merge of minor/patch bumps. Dependabot security alerts remain active
# via repo settings and feed into Renovate's vulnerabilityAlerts pipeline.
updates: []
